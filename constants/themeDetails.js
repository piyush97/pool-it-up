import { createTheme } from '@rneui/themed';

const theme = createTheme({
  lightColors: {},
  darkColors: {
    primary: '#0A84FF',
    background: '#1F2229',
    grey2: '#2E323B',
  },
});
export default theme;
